cmake_minimum_required(VERSION 3.13) 

project(CFastMath C)

set(CMAKE_C_STANDARD 99)
set(CMAKE_C_STANDARD_REQUIRED ON)

include(CheckIPOSupported)
check_ipo_supported(RESULT IpoSupported OUTPUT error)

if (IpoSupported)
      set(CMAKE_INTERPROCEDURAL_OPTIMIZATION ON)
endif()

add_library(CFastMath STATIC src/fastmath.c)

target_include_directories(CFastMath PUBLIC src)

if(MSVC)
      target_compile_options(CFastMath PRIVATE 
            /O2
            /fp:fast
            /Qvec
      )
else()
      target_compile_options(CFastMath PRIVATE 
            -O3 
            -ffast-math
            -fno-trapping-math 
            -funroll-loops 
            -falign-functions=64 
            -falign-loops=64 
            -march=native
      )
endif()